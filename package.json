{
  "name": "one-ui",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "start": "parcel index.html --global OneUi --out-dir ./tmp",
    "build": "parcel build src/index.js --global OneUi --out-dir ./dist ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^8.3.0",
    "parcel-bundler": "^1.7.1",
    "postcss-modules": "^6.0.0"
  },
  "dependencies": {
    "@webcomponents/custom-elements": "^1.1.1"
  }
}
